{
  "name": "gravityui-reference-mcp",
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc",
    "serve": "tsx src/server/server.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "update-submodules": "git submodule update --remote --depth 1",
    "extract": "tsx src/ingest/run-extract.ts",
    "overview": "tsx src/ingest/run-overview.ts",
    "validate": "tsx src/ingest/run-validate.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "minisearch": "^7.1.0",
    "zod": "^3.23.0"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^25.4.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
